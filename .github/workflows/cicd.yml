#this is name of the workflow which is displayed in the actions tab
name: CICD

on:
 workflow_dispatch:

jobs:

  code:
    runs-on: ubutu-latest
    steps:
       - name: checkout the code
         run: echo "checkout the code"
        
   build:
     runs-on: ubutu-latest
     steps:
       - name: "Build docker container"
         step: echo " build docker contianer"
   test:
      runs-on: ubutu-latest
     steps:
       - name: test the container
         run: echo "test the container "
   deploy:
     runs-on: ubutu-latest
     steps:
       - name: deploy the container
         step: echo "deploy the container"
  
